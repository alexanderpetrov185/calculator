// сложение
const plus = (a, b) => a + b;

// вычитание
const minus = (a, b) => a - b;

// деление
const division = (a, b) => a / b;

// умножение
const multiply = (a, b) => a * b;

// // проценты
// const percent = (b) => b / 100;
